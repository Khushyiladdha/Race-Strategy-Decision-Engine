# Base URL of the backend API. Leave unset for local dev (defaults to http://localhost:8000).
# On Vercel, set this to your Railway backend URL, e.g. https://your-backend.up.railway.app
VITE_API_BASE=
