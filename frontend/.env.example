# Base URL of the backend API. Leave unset for local dev (defaults to http://localhost:8000).
# On Vercel, set this to your Hugging Face Space URL, e.g. https://<user>-race-strategy.hf.space
VITE_API_BASE=
